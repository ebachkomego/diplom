{
  "buildCommand": "npm install --prefix client && npm run build --prefix client && mkdir -p public && cp -r client/dist/* public/",
  "framework": null,
  "env": {
    "SMTP_HOST": "smtp.gmail.com",
    "SMTP_PORT": "587",
    "SMTP_USER": "dysj1055@gmail.com",
    "SMTP_PASS": "LazArt25"
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.js" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
